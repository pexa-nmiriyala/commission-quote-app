# Only build/libs/*.jar is needed by the Dockerfile.
# Exclude everything else to keep the Docker context small and fast.
src/
gradle/
gradlew
gradlew.bat
build.gradle.kts
settings.gradle.kts
.gradle/
build/reports/
build/classes/
build/kotlin/
build/tmp/
build/generated/
build/resources/
