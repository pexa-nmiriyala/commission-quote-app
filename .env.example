# Commission API key — required in production.
# In dev, MockCommissionApiAdapter is used and this key is NOT sent to any external service.
# Set this environment variable before starting the server in production.
COMMISSION_API_KEY=
