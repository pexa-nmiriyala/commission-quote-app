# Commission API key — required in all environments.
# In dev/local profiles, MockCommissionApiAdapter is used and this key is NOT
# sent to any external service. The application will still fail to start if
# this is blank, to prevent misconfigured deployments.
COMMISSION_API_KEY=your-api-key-here
